apiVersion: v1
kind: Service

metadata:
  name: web-app-service
spec:
  type: NodePort
  ports:
    - name: "http"
      port: 80
      targetPort: 3000
      nodePort: 30001
  selector:
    app: web-app
